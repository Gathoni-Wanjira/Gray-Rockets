/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./Build/*.html"],
  theme: {
    extend: {},
  },
  plugins: [],
}

// The content inside [content] means that we want to run all files in the Build folder that are 
// html files.
